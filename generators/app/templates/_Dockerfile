FROM node

RUN apt-get update

ADD . /var/<%= organizationName %>/<%= serviceId %>
WORKDIR /var/<%= organizationName %>/<%= serviceId %>

RUN npm install --production

EXPOSE 9000

ENTRYPOINT npm start
